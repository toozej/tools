# GitHub Personal Access Token for API rate limit increase
# Generate a token at: https://github.com/settings/tokens
# Required scopes: none (public repos only) or repo (private repos)
GITHUB_TOKEN=

# Cache duration for GitHub API responses in milliseconds (default: 300000 = 5 minutes)
# Increase this to reduce API calls and avoid rate limits
GITHUB_CACHE_DURATION_MS=300000

# Polling interval for auto-refreshing the dashboard in milliseconds (default: 600000 = 10 minutes)
# This must be prefixed with NEXT_PUBLIC_ to be accessible in the browser
NEXT_PUBLIC_POLLING_INTERVAL_MS=600000